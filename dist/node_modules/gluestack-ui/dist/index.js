#!/usr/bin/env node
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
(function (factory) {
    if (typeof module === "object" && typeof module.exports === "object") {
        var v = factory(require, exports);
        if (v !== undefined) module.exports = v;
    }
    else if (typeof define === "function" && define.amd) {
        define(["require", "exports", "commander", "@clack/prompts", "./commands/add", "./commands/help", "./commands/init", "./commands/upgrade"], factory);
    }
})(function (require, exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    const commander_1 = require("commander");
    const prompts_1 = require("@clack/prompts");
    const add_1 = require("./commands/add");
    const help_1 = require("./commands/help");
    const init_1 = require("./commands/init");
    const upgrade_1 = require("./commands/upgrade");
    process.on('SIGINT', () => {
        prompts_1.log.warning('Operation ended.');
        process.exit(0);
    });
    process.on('SIGTERM', () => {
        prompts_1.log.warning('Operation cancelled.');
        process.exit(0);
    });
    function main() {
        return __awaiter(this, void 0, void 0, function* () {
            const program = new commander_1.Command().name('gluestack-ui');
            // Define action for unrecognized commands
            program.action(() => {
                prompts_1.log.error('\x1b[31mInvalid argument. Please specify a command. Use --help for assistance.\x1b[0m');
                process.exit(1);
            });
            program.addCommand(init_1.init).addCommand(add_1.add).addCommand(help_1.help).addCommand(upgrade_1.upgrade);
            program.parse();
        });
    }
    main();
});
